export interface IProduct {
	id: string;
	image: string;
	name: string;
	abstract: string;
	description: string;
	price: string;
	isFeatured: boolean;
	isActive: boolean;
	category: string;
	itemAmount: number;
}
